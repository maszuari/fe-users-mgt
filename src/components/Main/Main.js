import Box from '@mui/material/Box';
import '../DataTable/DataTableContainer'
import DataTableContainer from '../DataTable/DataTableContainer';

export default function Main() {
    return (
        <Box>
            <DataTableContainer />
        </Box>
    )
}